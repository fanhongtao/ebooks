\xiti

\begin{xiaotis}

\xiaoti{在下列各题中分别指出了一个集合的所有元素，用适当的方法把这个集合表示出来：}

\begin{xiaoxiaotis}
    
    \xiaoxiaoti{组成中国国旗图案的颜色；}

    \xiaoxiaoti{世界上最高的山山峰；}

    \xiaoxiaoti{由1，2，3这三个数字中抽出一部分或全部数字（没有重复）所排成的一切自然数；}

    \xiaoxiaoti{直角坐标系第一象限内所有的点的坐标。}

\end{xiaoxiaotis}

\xiaoti{写出方程 $x^2+x-1=0$ 的解集并进行化简。}

\xiaoti{写出方程组
$$
\begin{cases}
    x + y = 3, \\
    y + z = 4, \\
    z + x = 5
\end{cases}
$$
的解集并进行化简。
}

\xiaoti{在下列各题中，指出关系式 $A \subseteq B$，$A \supseteq B$，$A \subset B$，$A \supset B$，$A = B$ 中哪些成立：}

\begin{xiaoxiaotis}
    
    \xiaoxiaoti{$A=\{1,3,5,7\}$，$B=\{3,5,7\}$；}

    \xiaoxiaoti{$A=\{1,2,4,8\}$，$B=\{ x \mid x \text{是8的正约数}\}$。}

\end{xiaoxiaotis}

\xiaoti{判断下列各式是否正确，并说明理由：}

\begin{xiaoxiaotis}
    \begin{tabular}[t]{*{2}{@{}p{18em}}} 
        \xiaoxiaoti{$2 \subset \{x \mid x \leqslant 10\}$;} & \xiaoxiaoti{$2 \in\{x \mid x \leqslant 10\}$;} \\
        \xiaoxiaoti{$\{2\} \subset \{x \mid x \leqslant 10\}$;} & \xiaoxiaoti{$\kongji \in\{x \mid x \leqslant 10\}$;} \\
        \xiaoxiaoti{$\kongji \not\subset \{x \mid x \leqslant 10\}$;} & \xiaoxiaoti{$\kongji \subset \{x \mid x \leqslant 10\}$;} \\
        \xiaoxiaoti{$\{4,5,6,7\} \not\subset \{2,3,5,7,11\};$} & \xiaoxiaoti{$\{4,5,6,7\} \not\supset \{2,3,5,7,11\}。$}
    \end{tabular}
\end{xiaoxiaotis}

\xiaoti{学校里开运动会，设 $A=\{\text{参加百米赛跑的同学}\}$，$B=\{\text{参加跳高比赛的同学}\}$，求 $A \cap B$。}

\xiaoti{用适当的集合填空：}

\begin{center}
\begin{tabular}[t]{@{} c | *{3}{p{3em}}} 
    $\cap$ & $\kongji$ & $A$ & $B$ \\
    \hline
    $\kongji$ & \xhx & \xhx & \xhx \\
    $A$ & \xhx & \xhx & \xhx \\
    $B$ & \xhx & \underline{$A \cap B$} & \xhx
\end{tabular}
\end{center}

\xiaoti{设  $A=\{\text{红星农场的汽车}\}$，$B=\{\text{红星农场的拖拉机}\}$，求 $A \cup B$。}

\xiaoti{用适当的集合填空：}

\begin{center}
\begin{tabular}[t]{@{} c | *{3}{p{3em}}} 
    $\cup$ & $\kongji$ & $A$ & $B$ \\
    \hline
    $\kongji$ & \xhx & \xhx & \xhx \\
    $A$ & \underline{$\ A\quad$} & \xhx & \xhx \\
    $B$ & \xhx & \xhx & \xhx
\end{tabular}
\end{center}

\xiaoti{写出下列不等式的解集并进行化简：}

\begin{xiaoxiaotis}
    
    \twoInLine[18em]{\xiaoxiaoti{$x^2+2x-8 \leqslant 0$；}}{\xiaoxiaoti{$x^2+8x+15 > 0$。}}

\end{xiaoxiaotis}

\xiaoti{设 $S = \{x \mid x \leqslant 3\}$，$T = \{x \mid x < 1\}$，求 $S \cap T$ 及 $S \cup T$，并在数轴上表示出来。}

\xiaoti{写出不等式 $|3x-5|>2$ 的解集并进行化简。}

\xiaoti{用适当的集合填空：}


\begin{table}[htbp]
    \centering
    \begin{minipage}{6cm}
    \centering
    \begin{tabular}[t]{@{} c | *{3}{p{3em}}} 
        $\cap$ & $\kongji$ & $A$ & $\buji{A}$ \\
        \hline
        $\kongji$ & \xhx & \xhx & \xhx \\
        $A$ & \xhx & \xhx & \xhx \\
        $\buji{A}$ & \xhx & \xhx & \xhx
    \end{tabular}
    \end{minipage}
    \qquad
    \begin{minipage}{8cm}
    \centering
    \begin{tabular}[t]{@{} c | *{3}{p{3em}}} 
        $\cup$ & $\kongji$ & $A$ & $\buji{A}$ \\
        \hline
        $\kongji$ & \xhx & \xhx & \xhx \\
        $A$ & \xhx & \xhx & \xhx \\
        $\buji{A}$ & \xhx & \xhx & \xhx
    \end{tabular}
    \end{minipage}
\end{table}

\xiaoti{设 $I = \{x \mid x \in N \text{，且} x \leqslant 10\}$，
    $A=\{1,2,4,5,9\}$，$B=\{4,6,7,8,10\}$，$C=\{3,5,7\}$，
    求 $A \cap B$，$A \cup B$，$\buji{A} \cap \buji{B}$，
    $\buji{A} \cup \buji{B}$，$(A \cap B) \cap C$，
    $(A \cup B) \cup C$。
}

\xiaoti{设 $I=\{a,b,c,d,e,f\}$，$A=\{a,c,d\}$，$B=\{b,d,e\}$，
    求 $\buji{A}$，$\buji{B}$，$\buji{A} \cap \buji{B}$，$\buji{A} \cup \buji{B}$，
    $\buji{A \cap B}$，$\buji{A \cup B}$。
    看看求出的后四个集合中有没有相等的集合。
}

\xiaoti{设 $A = \{x \mid x^2-16<0\}$，$B = \{x \mid x^2-4x+3 \geqslant 0\}$}，$I = R$，求：

\begin{xiaoxiaotis}
    \begin{tabular}[t]{*{2}{@{}p{18em}}} 
        \xiaoxiaoti{$A \cap B$;} & \xiaoxiaoti{$A \cup B$;} \\
        \xiaoxiaoti{$\buji{A \cap B}$;} & \xiaoxiaoti{$\buji{A} \cup \buji{B}$。}
    \end{tabular}
\end{xiaoxiaotis}

\end{xiaotis}
